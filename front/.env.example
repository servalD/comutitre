# Front environment template (the front is not implemented in this iteration;
# these are the variables it will consume once auth/UI are wired up).
VITE_API_URL=http://localhost:3000
VITE_DYNAMIC_ENVIRONMENT_ID=your-dynamic-environment-id
